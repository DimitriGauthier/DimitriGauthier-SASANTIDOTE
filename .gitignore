# ── Dépendances / build (web/.gitignore couvre déjà le détail) ──
node_modules/
.next/
out/
build/
dist/

# ── Secrets — JAMAIS committer ──
.env
.env.*

# ── Supabase CLI (cache local) ──
supabase/.temp/
supabase/.branches/

# ── Docs, devis, assets internes : NE PAS publier sur le repo client ──
Documentation/
PDF/
prototype/
assets/
build_pdf.py
*.pdf

# ── OS / éditeurs ──
.DS_Store
Thumbs.db
.vscode/
.idea/
.claude/
